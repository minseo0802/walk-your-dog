// preload.js
// Expose any required APIs to renderer process here securely using contextBridge if needed.
window.addEventListener('DOMContentLoaded', () => {
  console.log('Preload script loaded successfully!');
});
